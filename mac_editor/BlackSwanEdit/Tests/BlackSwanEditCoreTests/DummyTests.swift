// Intentionally empty.
//
// This package supports running core tests via `swift run BlackSwanEditCoreTestRunner`.
// SwiftPM's `swift test` on macOS typically requires the XCTest framework from full Xcode.
